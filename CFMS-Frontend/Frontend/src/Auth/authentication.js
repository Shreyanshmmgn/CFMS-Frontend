import { useCookies } from "react-cookie";

const setCookie = (token) => {
  //   const [cookies, setCookie] = useCookies(["user"]);
  //   setCookie("", name, { path: "/" });
  //   setCookie("Password", pwd, { path: "/" });
  // const expires =
};
const logout = () => {};
const isLoggedIn = () => {};
const isLoggedOut = () => {};
const expiredOrNot = () => {};

export { setCookie, logout, isLoggedIn, isLoggedOut };
